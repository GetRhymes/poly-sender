import React from 'react';

const ExcelLists = ({setRootPath}) => {

    setRootPath("Списки")

    return (
        <div>
            <h1>ExcelLists</h1>
        </div>
    );
};

export default ExcelLists;